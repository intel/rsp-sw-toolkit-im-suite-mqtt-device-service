# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# service/control
gateway-device-service
gateway-device-service.docker

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

*.log

.idea/
vendor/
cmd/device-mqtt
glide.lock
